import { LegalPlaceholder } from "@/components/LegalPlaceholder";

export default function ShippingReturnsPage() {
  return (
    <LegalPlaceholder
      title="Shipping & Returns"
      note="This page will cover EU shipping zones and timelines, return windows, and the exchange process for print-on-demand items via Printify. Replace with full copy before launch."
    />
  );
}
